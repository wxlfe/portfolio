import { PortableTextPipe } from './portable-text.pipe';

describe('PortableTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PortableTextPipe();
    expect(pipe).toBeTruthy();
  });
});
