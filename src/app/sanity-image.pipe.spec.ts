import { SanityImagePipe } from './sanity-image.pipe';

describe('SanityImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SanityImagePipe();
    expect(pipe).toBeTruthy();
  });
});
